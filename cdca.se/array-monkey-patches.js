// Generated by CoffeeScript 1.8.0
Array.prototype.append = function(a) {
  var x, _i, _len;
  for (_i = 0, _len = a.length; _i < _len; _i++) {
    x = a[_i];
    this.push(x);
  }
  return this;
};

Array.prototype.prepend = function(a) {
  var x, _i, _len;
  for (_i = 0, _len = a.length; _i < _len; _i++) {
    x = a[_i];
    this.unshift(x);
  }
  return this;
};

Array.prototype.flatten = function(arr) {
  var item, _i, _len;
  if (arr == null) {
    arr = [];
  }
  for (_i = 0, _len = this.length; _i < _len; _i++) {
    item = this[_i];
    if (Array.isArray(item)) {
      item.flatten(arr);
    } else {
      arr.push(item);
    }
  }
  return arr;
};

Array.prototype.shuffle = function(randomFunc) {
  var index, item, length, rand, shuffled, _i, _len;
  if (randomFunc == null) {
    randomFunc = Math.random;
  }
  length = this.length;
  shuffled = new Array(length);
  for (index = _i = 0, _len = this.length; _i < _len; index = ++_i) {
    item = this[index];
    rand = Math.floor(randomFunc() * (index + 1));
    if (rand > index) {
      rand = index;
    }
    if (rand !== index) {
      shuffled[index] = shuffled[rand];
    }
    shuffled[rand] = this[index];
  }
  return shuffled;
};

//# sourceMappingURL=array-monkey-patches.js.map
